name: Protect main

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  pull_request_review:
    types: [submitted]

permissions:
  contents: read
  pull-requests: write
  statuses: read

jobs:
  protect:
    uses: b24cloud/github-actions/.github/workflows/workflow-protect-main.yml@main
    secrets: inherit
